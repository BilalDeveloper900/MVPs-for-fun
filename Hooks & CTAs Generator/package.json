{
  "name": "my-mvps-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for My MVPs project",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --recursive dev",
    "build": "pnpm --recursive build",
    "build:prod": "pnpm --recursive build:prod",
    "deploy:build": "pnpm run build:prod",
    "lint": "pnpm --recursive lint",
    "test": "pnpm --recursive test",
    "clean": "pnpm --recursive clean",
    "start:prod": "cd packages/api && npm run start:prod"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
